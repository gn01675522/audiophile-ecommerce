import HeroBanner from "@/components/server/HeroBanner/HeroBanner.component";

import type { FC } from "react";

const Home: FC = () => {
  return (
    <div>
      <section>
        <HeroBanner />
      </section>
    </div>
  );
};

export default Home;
